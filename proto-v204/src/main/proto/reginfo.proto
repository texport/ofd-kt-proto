syntax = "proto2";

option java_package = "kz.bfd.proto.v204";
package kz.bfd.proto.v204;

message KkmRegInfo {
  optional string point_of_payment_number = 1;
  optional string terminal_number = 2;
  optional string fns_kkm_id = 3; // required с версии 125 при command == COMMAND_CLOSE_SHIFT || command == COMMAND_REPORT
  optional string serial_number = 4; // required с версии 125 при command == COMMAND_CLOSE_SHIFT || command == COMMAND_REPORT
  optional string kkm_id =5; // required с версии 125 при command == COMMAND_CLOSE_SHIFT || command == COMMAND_REPORT
}

message PosRegInfo {
  required string title = 1; // required с версии 200
  required string address = 2; // required с версии 200
  required string address_kz = 3; // добавлен в версии 203
  required uint32 latitude = 4; // добавлен в версии 203
  required uint32 longitude = 5; // добавлен в версии 203
}

message OrgRegInfo {
  required string title = 1; // required с версии 200
  required string address = 2; // required с версии 200
  required string iin = 3; // required с версии 200
  required string oked = 5; // required с версии 200
  required string address_kz = 6; // добавлен в версии 203
}

message OfdRegInfo {
  required string title = 1; // required с версии 204
  required string title_kz = 2; // required с версии 204
  required string url = 3; // required с версии 204
}

